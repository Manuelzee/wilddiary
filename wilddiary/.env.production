# Set VITE_API_URL in the frontend host to the active backend URL ending in /api.
# Leaving it empty uses same-origin /api and avoids baking an expired preview URL
# into the production bundle.
VITE_API_URL=
